'use client'

import styled from 'styled-components'

export const DragonFormStyle = styled.div`
  display: grid;
  gap: 1rem;

  & .confirm-button {
    margin-top: 1.25rem;
  }

  & .action-form {
    display: flex;
    justify-content: flex-end;
  }
`
